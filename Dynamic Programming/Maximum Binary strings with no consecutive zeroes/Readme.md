Given an Integer n, the number of binary strings are 2<sup>n</sup>. Count the binary strings with no consecutive zeroes.

### Input
- n the length of binary string


### Output
Output a single integer, the max binary strings possible.
